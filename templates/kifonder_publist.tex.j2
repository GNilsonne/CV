\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{newtxtext}
\usepackage[margin=2.5cm]{geometry}
\usepackage{enumitem}
\usepackage[hyphens]{url}
\usepackage{xurl}
\usepackage[colorlinks,breaklinks=true]{hyperref}
\usepackage{lastpage}
\usepackage{fancyhdr}

\hypersetup{
    urlcolor=blue!70!black,
    linkcolor=blue!70!black,
}

\linespread{1.0}
\pagestyle{fancy}
\fancyhf{}
\rfoot{\thepage/\pageref{LastPage}}
\renewcommand{\headrulewidth}{0pt}
\setlist[enumerate]{leftmargin=2.5em, labelsep=0.5em, itemsep=0.5ex, parsep=0.3ex, topsep=0.5ex}
\sloppy

\begin{document}

\begin{center}
{\large\textbf{<< meta.name|tex >> -- Complete publication list << start_year >>--present}}
\end{center}

\section*{Peer-reviewed original research articles}
<% if originals %>
\begin{enumerate}
<% for pub in originals %>
\item <% if pub.authors %><< pub.authors|kif_authors|kif_bold_name >>: <% endif %><< pub.title|notrailingdot|tex >>. <% if pub.journal %>\textit{<< pub.journal|notrailingdot|tex >>}<% endif %><% if pub.volume %>,<< pub.volume >><% if pub.pages %>:<< pub.pages >><% elif pub.article_number %>:<< pub.article_number >><% endif %><% endif %><% if pub.year %>, << pub.year >><% endif %>.<% if pub.doi %> doi:~\href{https://doi.org/<< pub.doi >>}{<< pub.doi|tex >>}<% endif %>
<% endfor %>
\end{enumerate}
<% else %>
No entries found.
<% endif %>

\section*{Review articles}
<% if reviews %>
\begin{enumerate}
<% for pub in reviews %>
\item <% if pub.authors %><< pub.authors|kif_authors|kif_bold_name >>: <% endif %><< pub.title|notrailingdot|tex >>. <% if pub.journal %>\textit{<< pub.journal|notrailingdot|tex >>}<% endif %><% if pub.volume %>,<< pub.volume >><% if pub.pages %>:<< pub.pages >><% elif pub.article_number %>:<< pub.article_number >><% endif %><% endif %><% if pub.year %>, << pub.year >><% endif %>.<% if pub.doi %> doi:~\href{https://doi.org/<< pub.doi >>}{<< pub.doi|tex >>}<% endif %>
<% endfor %>
\end{enumerate}
<% else %>
No entries found.
<% endif %>

<% if other_articles %>
\section*{Other articles}
\begin{enumerate}
<% for pub in other_articles %>
\item <% if pub.authors %><< pub.authors|kif_authors|kif_bold_name >>: <% endif %><< pub.title|notrailingdot|tex >>. <% if pub.journal %>\textit{<< pub.journal|notrailingdot|tex >>}<% endif %><% if pub.volume %>,<< pub.volume >><% if pub.pages %>:<< pub.pages >><% elif pub.article_number %>:<< pub.article_number >><% endif %><% endif %><% if pub.year %>, << pub.year >><% endif %>.<% if pub.doi %> doi:~\href{https://doi.org/<< pub.doi >>}{<< pub.doi|tex >>}<% endif %>
<% endfor %>
\end{enumerate}
<% endif %>

\end{document}
